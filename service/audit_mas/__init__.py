"""solidity-auditor multi-agent service (v3.0.0).

The deployable twin of the Claude skill in ../SKILL.md. Same phase graph, same
finding schema, same reduction arithmetic — so a scorecard produced by either
half is comparable with the other.
"""

__version__ = "3.0.0"
